const apiRoot = '/api/v1/';
export const apiRoutes = {
  signUp: `${apiRoot}signup`,
  login: `${apiRoot}login`,
  channels: `${apiRoot}data`,
};
export const appRoutes = {
  login: 'login',
  signUp: 'signup',
};
